services:
  - type: web
    name: my-web-service
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ATLASDB_URL
        fromDatabase:
          name: my-database
          property: connectionString
